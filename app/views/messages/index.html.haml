- @messages.each do |message|
  .col-md-10.col-md-offset-1
    .message
      %h2.message-title
        = message.title
      = link_to "Join Convo", message, class: "btn-custom"


= link_to "Post Message", new_message_path, class: "btn btn-primary"
